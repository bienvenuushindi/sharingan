<nav class="fixed top-0 z-10 w-full h-full  flex flex-col justify-between items-start  shadow
        xl:w-64 xl:mt-0 xl:translate-x-0   bg-blue-900">
  <ul class="nav-group navbar-nav flex flex-col h-full  mr-10 mt-4 gap-3 w-full">
    <% if current_user.admin? %>
      <li><%= link_to 'Add Category', new_category_path, class: " py-3 px-5 hover:bg-black/40 text-white block font-medium" %></li>
      <li>  <%= link_to 'Add article', new_article_path, class: " py-3 px-5 hover:bg-black/40  text-white block font-medium" %></li>
      <li> <%= link_to 'Statistics', admin_root_path, class: " py-3 px-5 hover:bg-black/40 text-white block font-medium" %></li>
    <% end %>
    <li >
    <h4 class="px-5 text-white block font-medium text-gray-200">Search </h4>
      <ul class="ml-4">
        <% Category.parent_categories.each do |item| %>
          <% if item.name == 'Projects' %>
            <li><%= link_to item.name, search_by_category_path(category: item.name), class: " py-2 px-5 hover:bg-black/40  text-white block font-medium" %></li>
          <% else %>
            <li><%= link_to item.name, searches_path, class: " py-2 hover:bg-black/40 px-5  text-white block font-medium" %></li>
          <% end %>
        <% end %>
      </ul>
    </li>
    <li class="self-end mt-auto mx-auto">   <%= button_to 'Logout', destroy_user_session_path, method: :delete, class: 'btn bg-black text-white px-4 py-3 rounded ' %></li>
  </ul>
</nav>